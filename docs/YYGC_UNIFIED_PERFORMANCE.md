# YYGC 统一对象性能修正与验收

2026-09-13，接续 [U6 功能验收](YYGC_UNIFIED_IMPLEMENTATION.md)。YYGC 锁定 `745f3d2`。协议 7 有界压缩版本 `4e3798f` 已通过最终 144 项 Editor／Play、正式 Mono 完整矩阵 350 项，以及同一产物的 240 秒摘要容量检查 21 项。该窗口未再复现此前不断增长的投影积压。Sample 复用 `a7bb926`／同一 YYGC 的基础、弱网各 30 项证据；用户明确暂缓前台验收，性能仍未签署。各轮输入及原始失败证据分别保留。

## 已定位的热点

同一 Editor 中，对已冻结的 487,304 字节容量帧和只读 Worker Prefab 做同步微测量。每项预热一次、记录 12 批，原始样本保存在 `artifacts/yygc-unified/u6/performance/hotspot-*.json`。这些是定位数据，不是前台 Player 帧率。

| 操作 | 初次测量均值 | 用途 |
|---|---|---|
| 256 次真实装配校验 | 101.64 ms | 确认每次反射扫描 Behaviour 属性及绑定声明的成本 |
| 完整展示帧转换为 JSON | 39.93 ms | 确认验收驱动本身需要与正常运行负载分开 |
| MemoryPack 编码 | 1.62 ms | 记录当前完整投影的编码范围 |
| 解码、验证及冻结 | 3.69 ms | 与对象应用成本分开观察 |

仅缓存不可变反射声明后，256 次校验的两个观测均值为 16.66 ms 与 9.76 ms；环境负载会影响绝对值，不把这个变化直接换算为 Player 帧率改善。额外的类型解析缓存未显示明确收益，已撤回；最终框架修改仅保留 `ObjectAssemblyValidation.cs`。

## 本次修改

- YYGC 按 Behaviour 类型缓存所需配置类型、绑定键及组件类型。不缓存 Definition、配置实例、Unity 组件或校验成功结果；每次仍检查当前配置、工厂、能力、绑定唯一性和组件类型。
- 新回归先预热缓存，再移除配置、清空绑定、制造重复绑定，验证立即拒绝；恢复合法输入后可再次通过。
- MeasurementSeries 改为固定内存的全程直方图，不丢弃第 100,000 个样本以前的数据。分位数为桶上界，正值超过 0.000001 时相对误差不超过 1%；均值、总数和最大值仍覆盖全部样本。原尾部报告不改写。
- 客户端分别记录解码、对象准备、展示副本提交；Player 另记自动化报告耗时。`metrics-reset` 只清空诊断窗口，不修改游戏时间、状态、连接或 Ready。
- `report-detail` 可切换完整／摘要报告；容量脚本先完成完整对象、箭矢和 Ready 断言，再按参数测量摘要报告窗口，保留实际发布序号及计数。默认完整报告路径保留。
- 前台帧样本使用 Windows `GetForegroundWindow`／`GetWindowThreadProcessId`，区间两端均属于当前进程才计入；不把 Unity `isFocused` 当成 OS 证明。

## 验证顺序与边界

1. Unity 编译、相关装配与全程采样回归；原配置和资源不因性能修正重写。最终 138/138 通过；额外类型解析候选的 139/139 单独保留，候选已撤回。ArchitectureGuard 为 280 文件、12 自检、0 错误。
2. 锁定已验证框架提交，集中构建受影响的正式 Mono 与 Sample Mono。复用本次 U6 导入缓存，不复制 Library，不重试被自动审批拒绝的清理。
3. 同一正式 Player 执行受影响功能矩阵、完整／摘要两种容量报告负载，检查耗时、GC、发布频率和预热后的 OS 工作集。Sample 使用四进程回归。
4. 普通前台完整三夜及容量采样单独验收。用户在本轮明确选择“暂不显示，保留前台验收待办”，因此继续隐藏启动，不再重复请求显示窗口。IL2CPP 与双机器限制继续保留。

`test-game-pressure.ps1` 支持 `-CompactReports`、`-DurationSeconds`、`-ForegroundRole`；`test-game-campaign.ps1` 支持 `-ForegroundRole`。前台角色为 host／client1／client2／client3，默认 none。指定前台角色时，脚本要求该进程至少 95% 的帧区间有 OS 前台证据；不足则失败，不以其他进程或隐藏窗口数据代替。

延续 [开发计划](DEVELOPMENT.md)的可用 60 FPS 目标。U0 未冻结更细的性能预算，也无可信旧架构 Player 性能基线；本次先修复实测热点并提供完整条件，不宣称原架构对比优势。容量为合成投影，Host 只有 17 个真实权威对象，不代表 256 个 AI 的完整模拟。

## Mono 测量与长窗口积压

`a7bb926` 使用同一 Player 完成完整矩阵，Entry SHA-256 为 `94ECDC51399A6E8F97C6008D6FDEF5547AE2F8FFFD0FFD46E9EE26F8D8031535`。新三夜四端各保留 113,068–255,457 个帧样本，OS 前台样本均为 0；全程直方图已经生效，隐藏窗口帧时仍不作为前台成绩。

| 容量报告方式 | 观察窗口 | 客户端帧时 p95 | 每次 JSON 报告均值 | 客户端对象应用均值 |
|---|---|---|---|---|
| 完整报告 | 30 秒 | 50.70–53.29 ms | 40.89–42.63 ms | 14.95–15.10 ms |
| 摘要报告 | 90 秒 | 28.47–29.04 ms | 5.19–5.61 ms | 14.50–14.67 ms |

两次都完成 256 实体／1024 箭矢的完整接收和四人 Ready 检查。摘要报告只减少自动化 JSON，网络仍发送 487,304 字节完整投影。90 秒后客户端落后约 2.8–4.1 秒；Host 在约 79 秒的选定窗口增长 36 MiB。进一步的 240 秒观察证明问题持续：客户端落后从 0.8–1.6 秒增至 4.6–9.4 秒，Host 在预热后约 209 秒工作集增长 76.03 MiB、私有内存增长 83.46 MiB。客户端自身内存变化很小；不能将原 18 项容量功能检查解释为无积压或内存稳定。

首次长测的 UDP 中继在固定 240 秒期限退出，短于“启动准备＋240 秒观察”，造成断网。已保留失败报告，并给中继增加显式存活时长，容量入口传入观察时长加 240 秒准备／收尾余量，同时立即检测中继提前退出。修正后的 240 秒观察通过原 18 项检查，并记录上面的持续积压；没有通过增加连接超时掩盖游戏故障。

证据：`artifacts/yygc-unified/u6/performance/player-validation.json`、`artifacts/yygc-unified/u6/compression/backlog-baseline.json`，以及 `artifacts/migration/pressure-20260913-205705-066`、`pressure-20260913-210439-061`（工具期限失败）、`pressure-20260913-211501-495`。最后三个路径均从仓库根目录起算。

## 追加修正：协议 7 有界压缩

只读 Editor 探针将原 487,304 字节压缩为 10,594 字节；压缩／严格解码均值为 3.06／2.44 ms。该数据用于选择实现，不作为 Player 改善证明。

- 保留原 MemoryPack 完整投影和 YYGC 可靠同步链，在投影外增加 9 字节 `DNP` 版本 1 封套，标明原始／GZip 模式和未压缩长度。长度至少 1024 且压缩更小时才使用 GZip。
- 总包上限仍为 524,288 字节，未压缩体上限为 524,279 字节。解码分配前检查长度，验证 GZip 校验和、精确输出大小和压缩终点；拒绝截断、尾随字节和拼接成员。MemoryPack 字段与规则校验继续在解封之后执行。
- 游戏握手协议升至 7，明确拒绝协议 6／5；存档仍为 v2，YYGC 定义 wire 仍为 GuidV2。未增加依赖或修改 YYGC／FishNet。
- 原坏字段测试改为先放入合法封套，确保继续触发身份、NaN、规则键、事件和重复实体校验；另增加六个封套用例和协议 6 命令拒绝断言。
- 容量入口新增零丢包 LAN 的时效门槛：观察窗口中每 5 秒读取四端报告，各客户端的抽样落后不得超过 60 个服务端 tick（1 秒）。这是本轮明确新增的功能门槛，原报告不回填；它不是逐包延迟上界，详细前台性能预算仍未冻结。

首轮 Editor／Play 为 144/144，57.09 秒。架构检查要求把读取流拆到独立文件，修正后为 283 文件、12 自检、0 错误；最终 144/144 回归再次通过，57.63 秒。Sample 源码、依赖及其共用 YYGC 输入未变化，复用本轮已经通过的 Sample Mono／60 项证据，没有再次构建 Sample。

## 协议 7 的 Mono 验证结果

游戏输入 `4e3798fd4813eeb3c83fde092d51b96f5b74acfc`，框架输入 `745f3d2c844a66389e39bb84cd878d5d80f77962`，FishNet 仍为 `de19b5d`。复用 U6 已建立的隔离源码导入缓存，只构建一次正式 Mono；这次不是再次从无缓存目录导入，也没有复制 Library。CLI 于 `13:58:16.073Z` 至 `13:59:07.934Z` 完成，受保护源码／资源没有构建后差异。

Player 位于 `artifacts/yygc-unified/u6/player-mono-compressed/DarkNights.exe`，共 408 文件、199,003,190 字节。Entry SHA-256 为 `EAF2D63504DB826284CED9178E24E0CF7EE1D39E1C58A1C1E66B4FDEDBB2CD9C`。完整矩阵和长测之后逐文件重新核验，全部哈希一致。

| 同一产物的检查 | 结果 |
|---|---|
| 启动、定义及生成注册 | 6/6 |
| 双进程会话 | 13/13 |
| 并发、权限及去重 | 13/13 |
| 活跃施工／训练／箭矢恢复 | 14/14 |
| 四人保存、恢复、重开及退出 | 24/24 |
| 九组真实 UDP 弱网 | 每组 24/24，共 216 项 |
| 两分辨率捕获及异常检查 | 各 6/6，共 12 项；不代替视觉签署 |
| 战斗及第二夜晚加入 | 9/9 |
| 四进程完整三夜 | 22/22 |
| 30 秒完整报告容量及新增时效检查 | 21/21 |
| 完整矩阵合计 | **350 项通过** |
| 另行执行的 240 秒摘要容量观察 | **21/21**，与上行容量检查属于同一组用例的更长窗口 |

完整入口为 `artifacts/migration/delivery-20260913-220231-851/result.json`，长测为 `artifacts/migration/pressure-20260913-222439-382/result.json`。机器汇总见 [协议 7 证据](evidence/yygc-unified-u6-compression.json)；原始分段计时、全程采样及各步骤哈希在 `artifacts/yygc-unified/u6/compression/player-validation.json`，OS／托管内存窗口在同目录 `retention-assessment.json`。

### 容量时效与内存观察

容量仍是 256 实体／1024 箭矢的合成完整投影；Host 仅有 17 个真实权威对象，观察期间已暂停模拟，不代表 256 个 AI 的模拟性能。摘要模式只减少自动化 JSON 报告，不减少网络内容、对象数量或箭矢数量。两次报告末尾的投影包均为 **10,604 字节**，原始体探针为 487,304 字节。

| 报告方式 | 观察时长 | 客户端后台帧时 p95 | 解码均值 | 对象应用均值 | JSON 报告均值 |
|---|---|---|---|---|---|
| 完整 | 30.45 秒 | 47.29–48.72 ms | 4.48–4.70 ms | 13.63–13.89 ms | 39.38–40.06 ms |
| 摘要 | 240.85 秒 | 29.33–29.62 ms | 4.48–4.72 ms | 14.32–14.58 ms | 4.93–5.05 ms |

240 秒窗口内，publication 从 261 增至 2668，增加 2407，超过门槛 1200；四端保持 Ready。48 组抽样中，三个客户端最大落后分别为 **12／12／18 tick，即 0.2／0.2／0.3 秒**，均通过新增的一秒门槛。相比原 240 秒观察中持续增加到 4.6–9.4 秒的落后，本次未复现该积压现象。UDP 中继在包括准备与收尾的 275.24 秒内记录服务端向客户端 82,261,282 字节，无丢包或提前退出；该总量不冒充只覆盖测量窗口的速率。

OS 内存取测量开始 10 秒后的样本，每端 46 项，实际首末跨度约 229.65 秒：

| 角色 | 工作集首末变化 | 私有内存首末变化 | 最后约 56.75 秒私有内存变化 |
|---|---:|---:|---:|
| Host | +0.191 MiB | +0.043 MiB | +0.453 MiB |
| client1 | −0.059 MiB | −0.035 MiB | 0 MiB |
| client2 | +1.199 MiB | +0.523 MiB | 0 MiB |
| client3 | +1.051 MiB | −0.016 MiB | 0 MiB |

Host 不再呈现旧窗口中工作集 +76.03 MiB／私有内存 +83.46 MiB 的增长。托管已用内存按各进程 `metrics-reset` 后 10 秒取样，窗口与 OS 采样不完全重合；Host 在 31.39–41.89 MiB 之间波动，末 55 秒首末减少约 2.16 MiB。原值与斜率均保留，不能凭这四分钟观察签署长期无泄漏。完整报告短测仍有约 40 ms 的报告开销，客户端对象应用也仍约 14 ms，前台性能需要单独验收。

完整三夜各端保留 115,627–259,756 个帧样本，所有计数均与保留计数相等；三夜和两组容量测试的 OS 前台样本均为 0。按用户“暂不显示，保留前台验收待办”的选择，本轮未启动可见窗口，不签署 60 FPS。M5 字体／菜单、IL2CPP 和双机器 LAN 的边界继续保留。

## 空间与证据

阶段开始 C／D 盘分别约 13.7／22.7 GiB 可用。新的探针在 Assets 外以内存方式编译，只输出小型 JSON；生成的 `.meta` 由 Unity 批量导入。保留之前的 Player、失败与成功报告；阶段结束执行工具自身的 clean，并如实记录被审批拦截的缓存仍未释放。

压缩切片已用 `dotnet clean` 再次释放工具中间产物 **32,992,560 字节**；当前源码工作目录仍复用 U6 的导入缓存，新增 Player 输出使用独立空目录。被自动审批拒绝的 source／locked-archives、旧 Bee 及旧 Player 调试目录清理未重试。

长测结束后，核验新 Player 调试目录位于本任务输出内、无重解析链接、无活动 Player 且文件对应已验证清单，再请求清理 `player-mono-compressed/DNights_BurstDebugInformation_DoNotShip`。自动审批仍以 `blocked by policy` 拒绝，命令未执行；该目录的 1 文件、245,875 字节保持原样，释放量记为 0，不换方式重试。记录在 `compression/debug-cleanup-preflight.json`／`debug-cleanup.json`。结束记录 C／D 可用空间约 **14.57／23.31 GB**（十进制）；仍保留完整 Player 供后续前台验收，不把被拒绝的清理写成完成。
